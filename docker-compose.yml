version: '3.8'

services:
  # Backend Service (Express + TypeScript)
  backend:
    image: b4869/my-devops:backend_v1.0
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: backend-app
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      # Mount logs directory เพื่อเก็บ logs ไว้นอก container
      - ./backend/logs:/app/logs
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    restart: unless-stopped
    networks:
      - app-network

  # Frontend Service (Next.js + Nginx)
  frontend:
    image: b4869/my-devops:frontend_v1.0
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: frontend-app
    ports:
      - "8080:80"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:3000
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - app-network

# Network สำหรับให้ services คุยกันได้
networks:
  app-network:
    driver: bridge

# Volumes (ถ้าต้องการเก็บข้อมูลถาวร)
volumes:
  backend-logs:
